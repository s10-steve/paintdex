## What this changes

<!-- Describe the change. For a paint-data correction, name the brand/paint and cite your source (manufacturer swatch, physical sample, etc.) — see data/paints/README.md. -->

## Checklist

- [ ] `npm run lint` passes
- [ ] `npm run test` passes
- [ ] `npm run validate:data` passes (if `data/paints/*.json` changed)
- [ ] `npm run build` passes
- [ ] For colour-value changes: a source is cited above
